# Study DB 実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 学習セッションを Notion Study DB + ローカル MD で管理し、`/study` スキルで対話的にノートを取れるようにする。

**Architecture:** `scripts/lib/notion.ts` に `study` DB を追加し、`notion-add.ts --db study` で登録できるようにする。`/study` スキルが Notion 登録・MD 作成・対話ノート取りの一連のフローを担当する。

**Tech Stack:** TypeScript / Bun、Notion MCP (`notion-update-page`、`notion-create-database`)、Claude skills (Markdown)

**Spec:** `docs/superpowers/specs/2026-03-18-study-db-design.md`

---

## File Map

| ファイル | 操作 | 内容 |
|---------|------|------|
| `scripts/lib/notion.ts` | 修正 | `ScheduleDbName` に `study` 追加、`SCHEDULE_DB_CONFIGS` にエントリ追加 |
| `scripts/validate-entry.ts` | 修正 | `DB_LABELS` に `study: '学習'` 追加 |
| `.env.local` | 修正 | `NOTION_STUDY_DB=<db-id>` 追加 |
| `.claude/skills/study/SKILL.md` | 新規作成 | `/study` コマンドの全フロー定義 |
| `aspects/study/startup/notes/.gitkeep` | 新規作成 | ディレクトリ作成 |
| `aspects/study/law/notes/.gitkeep` | 新規作成 | ディレクトリ作成 |
| `aspects/study/tech/notes/.gitkeep` | 新規作成 | ディレクトリ作成 |
| `aspects/study/other/notes/.gitkeep` | 新規作成 | ディレクトリ作成 |
| `.claude/rules/notion-workflow.md` | 修正 | Study DB をテーブルと重複バリデーション対象リストに追記 |
| `aspects/study/README.md` | 修正 | 「学習セッションの記録」セクション追加 |

---

## Task 1: Notion Study DB を作成して ID を取得する

**Files:**
- Modify: `.env.local`

- [ ] **Step 1: Notion MCP で Study DB を作成する**

  `notion-create-database` を使って以下のスキーマで DB を作成する。親ページは既存の Notion ワークスペース（他の DB と同じ場所に置く）。

  ```
  DB名: Study
  プロパティ:
    - 名前 (title)
    - 日付 (date)
    - カテゴリ (select): algorithms, system-design, startup, law, tech, other
    - 本 (select): ← 空で作成、後から追加
    - Chapter (rich_text)
  ```

  ※ ステータスプロパティは作らない

- [ ] **Step 2: 作成した DB の ID を `.env.local` に追加する**

  ```
  NOTION_STUDY_DB=<取得したDB ID>
  ```

- [ ] **Step 3: コミット**

  ```bash
  git add .env.local
  git commit -m "chore: add NOTION_STUDY_DB to env"
  ```

---

## Task 2: `scripts/lib/notion.ts` に study DB を追加する

**Files:**
- Modify: `scripts/lib/notion.ts:73` (`ScheduleDbName` 型)
- Modify: `scripts/lib/notion.ts:85-94` (`SCHEDULE_DB_CONFIGS`)

- [ ] **Step 1: `ScheduleDbName` に `study` を追加する**

  `scripts/lib/notion.ts` の73行目:

  ```typescript
  // 変更前
  export type ScheduleDbName = "devotion" | "events" | "guitar" | "sound" | "meals" | "groceries" | "todo" | "other";

  // 変更後
  export type ScheduleDbName = "devotion" | "events" | "guitar" | "sound" | "meals" | "groceries" | "todo" | "other" | "study";
  ```

- [ ] **Step 2: `SCHEDULE_DB_CONFIGS` に `study` エントリを追加する**

  `scripts/lib/notion.ts` の85-94行目の `SCHEDULE_DB_CONFIGS` オブジェクトに追加:

  ```typescript
  export const SCHEDULE_DB_CONFIGS: Record<ScheduleDbName, ScheduleDbConfig> = {
    devotion: { ... },
    events:  { ... },
    // ... 既存エントリはそのまま ...
    other:   { envKey: "NOTION_OTHER_DB", titleProp: "名前", dateProp: "日付", descProp: "" },
    // ↓ 追加
    study:   { envKey: "NOTION_STUDY_DB", titleProp: "名前", dateProp: "日付", descProp: "" },
  };
  ```

- [ ] **Step 3: TypeScript が通ることを確認する**

  ```bash
  bun run typecheck
  ```

  Expected: エラーなし

- [ ] **Step 4: `notion-add.ts --db study` が動作することを確認する（ドライラン）**

  ```bash
  bun run scripts/notion-add.ts --title "勉強" --date 2026-03-18 --start 14:00 --end 15:00 --db study
  ```

  Expected: Notion Study DB にエントリが作成され、`追加しました: 勉強 [study]` が出力される

  ※ 動作確認後、テスト用エントリは `bun run scripts/notion-delete.ts <page-id>` で削除する

- [ ] **Step 5: コミット**

  ```bash
  git add scripts/lib/notion.ts
  git commit -m "feat: add study DB to ScheduleDbName and SCHEDULE_DB_CONFIGS"
  ```

---

## Task 3: `scripts/validate-entry.ts` の `DB_LABELS` に `study` を追加する

**Files:**
- Modify: `scripts/validate-entry.ts:17-20`

- [ ] **Step 1: `DB_LABELS` に `study: '学習'` を追加する**

  `scripts/validate-entry.ts` の17-20行目:

  ```typescript
  // 変更前
  const DB_LABELS: Record<ScheduleDbName, string> = {
    devotion: "デボーション", events: "イベント", todo: "やること",
    guitar: "ギター", sound: "音響", meals: "食事", groceries: "買い出し", other: "その他",
  };

  // 変更後
  const DB_LABELS: Record<ScheduleDbName, string> = {
    devotion: "デボーション", events: "イベント", todo: "やること",
    guitar: "ギター", sound: "音響", meals: "食事", groceries: "買い出し", other: "その他",
    study: "学習",
  };
  ```

- [ ] **Step 2: TypeScript が通ることを確認する**

  ```bash
  bun run typecheck
  ```

  Expected: エラーなし

- [ ] **Step 3: validate-entry.ts が study DB の重複を検出することを確認する**

  Task 2 Step 4 で作成したテスト用エントリがまだある場合:

  ```bash
  bun run scripts/validate-entry.ts --date 2026-03-18 --title "勉強" --start 14:00 --end 15:00
  ```

  Expected: `⚠️ 類似エントリ検出: [学習] "勉強" ...` と出力され、終了コード 1

- [ ] **Step 4: コミット**

  ```bash
  git add scripts/validate-entry.ts
  git commit -m "feat: add study label to validate-entry DB_LABELS"
  ```

---

## Task 4: カテゴリ別ディレクトリを作成する

**Files:**
- Create: `aspects/study/startup/notes/.gitkeep`
- Create: `aspects/study/law/notes/.gitkeep`
- Create: `aspects/study/tech/notes/.gitkeep`
- Create: `aspects/study/other/notes/.gitkeep`

- [ ] **Step 1: 不足しているディレクトリを作成する**

  ```bash
  mkdir -p aspects/study/startup/notes
  mkdir -p aspects/study/law/notes
  mkdir -p aspects/study/tech/notes
  mkdir -p aspects/study/other/notes
  touch aspects/study/startup/notes/.gitkeep
  touch aspects/study/law/notes/.gitkeep
  touch aspects/study/tech/notes/.gitkeep
  touch aspects/study/other/notes/.gitkeep
  ```

  ※ `algorithms/notes/` と `system-design/notes/` はすでに存在する

- [ ] **Step 2: コミット**

  ```bash
  git add aspects/study/
  git commit -m "feat: add study category directories"
  ```

---

## Task 5: `/study` スキルファイルを作成する

**Files:**
- Create: `.claude/skills/study/SKILL.md`

- [ ] **Step 1: スキルファイルを作成する**

  `.claude/skills/study/SKILL.md` を以下の内容で作成する:

  ````markdown
  ---
  name: study
  description: 学習セッションの開始・ノート記録・Notion登録。引数: $ARGUMENTS
  ---

  # study — 学習セッション管理

  ## 引数パース

  `$ARGUMENTS` を確認する:
  - `<カテゴリ>` のみ → そのカテゴリでセッション開始
  - `<カテゴリ> --start HH:MM` → カテゴリ + 開始時刻指定
  - 引数なし → 対話でカテゴリを確認する

  カテゴリ選択肢: `algorithms` / `system-design` / `startup` / `law` / `tech` / `other`

  ---

  ## Step 1: 情報収集

  未指定の情報をユーザーに確認する（一度にまとめて確認してよい）:

  1. **カテゴリ**（必須）: algorithms / system-design / startup / law / tech / other
  2. **開始時刻**（必須）: `TZ=Asia/Tokyo date` で現在時刻を確認し、未指定ならユーザーに確認
  3. **終了時刻**（任意）: 未定なら「後で更新」として進める
  4. **本**（任意）: 参照する本のタイトル
  5. **Chapter**（任意）: 章・節

  ---

  ## Step 2: 重複チェック

  ```bash
  TZ=Asia/Tokyo date +%Y-%m-%d  # 今日の日付を確認
  bun run scripts/validate-entry.ts --date YYYY-MM-DD --title "勉強" --start HH:MM --end HH:MM
  ```

  - **終了コード 1**（類似エントリあり）→ 既存エントリの内容を `notion-fetch` で確認してユーザーに提示し、以下を確認する:
    - **上書き**: 既存エントリを削除して新規作成
    - **追記**: 既存エントリのノートに追記する形で継続（Step 3 をスキップして既存 notion_id を使う）
  - **終了コード 0** → 次のステップへ

  ---

  ## Step 3: Notion 登録（2ステップ）

  ### Step 3a: `notion-add.ts` でページ作成

  ```bash
  bun run scripts/notion-add.ts --title "勉強" --date YYYY-MM-DD --start HH:MM --end HH:MM --db study
  ```

  出力から page ID を取得する。

  ### Step 3b: `notion-update-page` でプロパティ + ページ本文を設定

  Notion MCP の `notion-update-page` を使って以下を設定する:

  **プロパティ設定:**
  ```
  select:カテゴリ: <カテゴリ名>
  select:本: <本のタイトル>  (あれば)
  rich_text:Chapter: <章・節>  (あれば)
  cover:
    type: "external"
    external.url: "https://www.notion.so/images/page-cover/gradients_2.png"
    (または他のグラデーション/単色カバー)
  ```

  **ページ本文（`replace_content`）:**

  ```
  [callout] 📚
  テキスト: 📅 YYYY-MM-DD  HH:MM → HH:MM  |  🏷 {カテゴリ}  |  📗 {本}（あれば）  |  📌 {Chapter}（あれば）

  [divider]

  [heading_2] 🎯 今日の目標・疑問

  [paragraph] （ユーザーから聞いた目標・疑問をここに書く）

  [divider]

  [heading_2] 📝 ノート

  [paragraph] （セッション中に追記）

  [divider]

  [heading_2] 🔑 キーワード

  [paragraph] （重要用語・概念）

  [divider]

  [heading_2] 💡 まとめ

  [paragraph] （セッション終了時に記入）

  [divider]

  [heading_2] ❓ 残った疑問・次回へ

  [paragraph] （理解できなかった点、次のセッションで深めたいこと）
  ```

  ---

  ## Step 4: ローカル MD を作成する

  ### ファイルパスの決定

  - 本あり: `aspects/study/{category}/notes/YYYY-MM-DD-{book-slug}.md`
  - 本なし: `aspects/study/{category}/notes/YYYY-MM-DD.md`
  - 同日同カテゴリ同本で既存ファイルがある場合: `-2.md`、`-3.md` と連番

  book-slug ルール: タイトルを小文字ケバブケースに変換（例: `cracking-the-coding-interview`）

  ### MD 内容

  ```markdown
  ---
  notion_id: <page-id>
  date: YYYY-MM-DD
  start: HH:MM
  end: HH:MM
  category: <カテゴリ>
  book: <本のタイトル>
  chapter: <Chapter>
  ---

  # 勉強 - YYYY-MM-DD

  ## 🎯 今日の目標・疑問

  <ユーザーから聞いた目標・疑問>

  ## 📝 ノート

  （セッション中に追記）

  ## 🔑 キーワード

  （重要用語・概念）

  ## 💡 まとめ

  （セッション終了時に記入）

  ## ❓ 残った疑問・次回へ

  （理解できなかった点、次のセッションで深めたいこと）
  ```

  ### キャッシュクリア

  ```bash
  bun run scripts/cache-status.ts --clear
  ```

  ---

  ## Step 5: セッション開始

  ユーザーに伝える:

  ```
  セッションを開始しました 📖
  📅 YYYY-MM-DD  HH:MM〜
  🏷 {カテゴリ}  📗 {本}（あれば）

  今日の目標・疑問は何ですか？
  ```

  ユーザーの回答を受け取り、MD と Notion の「🎯 今日の目標・疑問」セクションに書き込む。

  ---

  ## Step 6: 対話セッション（ノート取り）

  - ユーザーの質問・学習内容に答えながら、**会話の要点を Claude が整理して MD の「📝 ノート」に随時書き込む**
  - 重要な用語・概念が出たら「🔑 キーワード」にも追記する
  - Notion への書き込み: ノートが一定量溜まったとき、またはユーザーが「メモして」「Notionに書いて」と言ったとき
  - Notion 書き込みには `notion-update-page`（`replace_content`）を使い、MD の内容全体を反映する（差分ではなく全文置き換え）

  **同期ルール（厳守）:**
  - MD と Notion の内容は常に一致させる
  - Notion に書き込んだ後、MD も同じ内容に更新する（逆も同様）
  - 片方だけの更新で終わらせない

  ---

  ## Step 7: セッション終了

  「終わり」「完了」「終了」「おわり」「セッション終了」のいずれかが来たら:

  1. **終了確認をする**:
     ```
     セッションを終了してよいですか？まとめと残った疑問を一緒に整理してから閉じます。
     ```

  2. ユーザーが確認したら、まとめと残った疑問を一緒に作成する:
     - 「今日学んだことを3点でまとめると？」などと問いかけて内容を引き出す
     - Claude がまとめ文を作成してユーザーに確認してもらう

  3. **MD を最終内容で更新する** （終了時刻 + まとめ + 残った疑問）

  4. **Notion に最終内容を同期する** (`notion-update-page` の `replace_content` で MD 全体を反映)
     - 終了時刻も更新: `date:日付:end: YYYY-MM-DDThh:mm:00+09:00`

  5. **キャッシュクリア**:
     ```bash
     bun run scripts/cache-status.ts --clear
     ```

  6. **完了報告**:
     ```
     セッション終了 ✅

     📅 YYYY-MM-DD  HH:MM〜HH:MM
     🏷 {カテゴリ}  📗 {本}（あれば）

     Notion ✅ / ローカル MD ✅
     📄 aspects/study/{category}/notes/{filename}.md
     ```
  ````

- [ ] **Step 2: スキルがリストに表示されることを確認する**

  Claude Code の設定で `/study` コマンドが認識されることを確認する（Claude Code を再起動するか、新しいセッションで `/study` を入力して確認）。

- [ ] **Step 3: コミット**

  ```bash
  git add .claude/skills/study/SKILL.md
  git commit -m "feat: add /study skill for interactive study sessions"
  ```

---

## Task 6: ドキュメントを更新する

**Files:**
- Modify: `.claude/rules/notion-workflow.md:74-81`（Schedule DBs テーブル）と `104`（重複バリデーションリスト）
- Modify: `aspects/study/README.md`

- [ ] **Step 1: `notion-workflow.md` の Schedule DBs テーブルに Study DB を追加する**

  `.claude/rules/notion-workflow.md` の74-81行目のテーブルに追加:

  ```markdown
  | 学習     | `NOTION_STUDY_DB`     | 名前 / 日付 / カテゴリ / 本 / Chapter | なし | 学習セッション・ノート |
  ```

- [ ] **Step 2: 重複バリデーション対象リストに `study` を追加する**

  `.claude/rules/notion-workflow.md` の104行目:

  ```markdown
  # 変更前
  Notion にスケジュール系エントリ（devotion / events / todo / meals / groceries / guitar / sound）を

  # 変更後
  Notion にスケジュール系エントリ（devotion / events / todo / meals / groceries / guitar / sound / study）を
  ```

- [ ] **Step 3: `aspects/study/README.md` に `/study` コマンドの説明を追加する**

  `aspects/study/README.md` の末尾に追加:

  ```markdown
  ## 学習セッションの記録

  `/study` コマンドで学習セッションを開始できます。

  - Notion Study DB にセッションを登録（カレンダー連携）
  - Claude と対話しながらコーネル式ノートを記録
  - ローカル MD と Notion ページを同期管理
  - ファイルパス: `aspects/study/{category}/notes/YYYY-MM-DD-{book-slug}.md`

  ### 使い方

  ```bash
  /study                          # 対話式でカテゴリ・時刻を確認
  /study algorithms               # カテゴリ指定
  /study algorithms --start 14:00 # 開始時刻も指定
  ```
  ```

- [ ] **Step 4: コミット**

  ```bash
  git add .claude/rules/notion-workflow.md aspects/study/README.md
  git commit -m "docs: update notion-workflow and study README for Study DB"
  ```

---

## Task 7: 動作確認

- [ ] **Step 1: `/study` コマンドを実際に実行して一通りのフローを確認する**

  新しい Claude Code セッションで:
  ```
  /study algorithms --start 14:00
  ```

  確認ポイント:
  - 情報収集（本・Chapter の確認）ができる
  - Notion Study DB にエントリが作成される（アイコン 📖・カバー・カテゴリ設定）
  - ローカル MD が `aspects/study/algorithms/notes/YYYY-MM-DD.md` に作成される
  - セッション中の対話でノートが MD に書き込まれる
  - 「終わり」でセッション終了確認が出る
  - まとめ作成後、Notion と MD 両方が最終内容で更新される

- [ ] **Step 2: テスト用エントリを削除する**

  ```bash
  bun run scripts/notion-delete.ts <test-page-id>
  ```

  作成した MD ファイルも削除する（テスト用のため）。

- [ ] **Step 3: 最終コミット・PR 作成**

  ```bash
  /pr
  ```
