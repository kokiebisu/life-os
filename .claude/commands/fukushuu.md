# 復習 - 忘却曲線ベーススペーシドリピティション

`aspects/study/` の学習ノートを忘却曲線に基づいて復習する。

## 復習間隔

| 累計復習回数 | 次回まで |
|---|---|
| 未復習 | 即座に対象 |
| 1回目後 | 1日 |
| 2回目後 | 3日 |
| 3回目後 | 7日 |
| 4回目後 | 14日 |
| 5回目後 | 30日 |
| 6回目以降 | 60日 |

「覚えていなかった（n）」→ review_count をリセット・interval を1日に戻す。

## Step 1: 今日の日付を確認

```bash
TZ=Asia/Tokyo date +%Y-%m-%d
```

## Step 2: 復習ログを読む

`aspects/study/review-log.json` を Read する。ファイルがなければ空の `{}` として扱う。

## Step 3: 対象ノートを特定

Glob で `aspects/study/**/*.md` を取得し、以下を**除外**する:

- `**/CLAUDE.md`
- `**/README.md`
- `**/roadmap.md`
- `aspects/study/team/**`

各ノートについて、review-log.json と今日の日付を照合して「期日が来ているか」を判定する:

```
interval_map = { 0: 0, 1: 1, 2: 3, 3: 7, 4: 14, 5: 30, それ以降: 60 }
due = (last_reviewed + interval_days <= today) または (ログになければ即対象)
```

期日が古い順にソートし、**最大5件**を選ぶ。

対象ノートが0件の場合は「今日復習すべきノートはありません ✅ 次の復習は〇〇日（最も近い期日）です」と伝えて終了。

## Step 4: 対話的クイズ

対象ノートを1件ずつ処理する。

### 各ノートの処理

1. ノートを Read する
2. タイトルと `🧒 一言で言うと` を表示する（答えを先に見せない）
3. `❓ 自分への質問（コーネル式キュー）` の質問を **1問ずつ** 出題する
   - ユーザーが答えるのを待つ
   - 答えが返ってきたら、ノートの該当箇所を引用して解説・補足する
   - 次の質問がある場合は続ける
4. 全問終了後:「このノート、覚えてた？ (y/n)」と聞く
5. 回答に応じて次回スケジュールを伝える:
   - y → 「次回は〇〇日です」
   - n → 「もう一度、明日復習しましょう」

### コーネル式キューがないノートの場合

`❓ 自分への質問` セクションが存在しない、または空のとき:
- `🧒 一言で言うと` を隠した状態でノートのタイトルだけ見せ「このトピック、何を学んだか説明してみて」と問いかける
- ユーザーの説明後に `一言で言うと` と主要ポイントを表示する

## Step 5: review-log.json を更新

全ノートの処理が終わったら `aspects/study/review-log.json` を Write で更新する。

```json
{
  "aspects/study/system-design/fundamentals/scale-from-zero.md": {
    "last_reviewed": "YYYY-MM-DD",
    "interval_days": 3,
    "review_count": 2
  }
}
```

- `last_reviewed`: 今日の日付（JST）
- `review_count`: y なら +1、n なら 0 にリセット
- `interval_days`: 上記の interval_map から review_count に対応する値

## 注意

- 1問ずつ出す。複数の質問を一度に表示しない
- ユーザーの答えを待つ前にノートの内容を開示しない
- セッション中に新しいノートが追加されていても対象に含める
